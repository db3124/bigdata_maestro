# 숫자를 다루는 함수
x <- c(1, 2, 3)
x

# 함수 적용
mean(x)

# 최댓값
max(x)

# 최솟값
min(x)

y <- c(1:10)
y

# 합
sum(y)

# 중간값
median(y)

# various: 분산
var(y)

# standard deviation: 표준편차
sd(y)

z <- c(10:1)
z

# sort: 정렬
z <- sort(z)
z

# 값의 범위
range(z)

# 벡터에 저장된 값의 개수
length(z)
