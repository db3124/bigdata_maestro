# 변수명 생성 규칙
# 문자(알파벳)와 .로 시작
# 문자, 숫자, 온점(.), 언더바(_)를 조합하여 만듦
score <- 90
# 3score <- 80
# _score <- 100
# -score <- 90
.score <- 70 # 가능하나 권장하지 않음. 온점은 hidden file을 뜻하기 때문

변수 <- 'cho'
변수 # 오류

my_first.name2 <- 'longlee'
my_first.name2

name <- 'cho'
NAME <- 'CHO'

# for <- 30 # 예약어는 사용불가