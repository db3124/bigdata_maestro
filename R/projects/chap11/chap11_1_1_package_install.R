# 단계 구분도:지역별 통계치를 색깔의 차이로 표현한 지도

# ggiraphExtra를 위한 패키지 설치
install.packages("mapproj")

# 단계 구분도 만들기 위한 패키지
# ggplot2를 이용해 쉽게 지도를 그릴 수 있게 하는 패키지
install.packages("ggiraphExtra")

# 미국 주 지도 데이터(위도, 경도, 지역 등) 준비 패키지
install.packages("maps")
